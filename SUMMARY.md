# 项目摘要

BiliAssist 是纯 Rust 原生桌面应用。GPUI 负责窗口与渲染，gpui-component 提供输入框、按钮、开关、标签页和滚动容器；Tokio 后台服务持续处理 B站事件。

应用包含两层账号：

- Supabase 应用账号控制登录状态与 Basic/Plus 等级。
- B站账号通过二维码添加，Cookie 使用本地 AES-256-GCM 加密。

自动回复按视频评论、动态评论、私信和关注四个渠道独立配置。视频评论同时覆盖一级评论、子评论和用户指定的 BV 视频；指定视频可单独设置回复文案、策略和自动点赞。所有渠道共用可调检查间隔，并将成功记录及时写回界面和本地文件。

构建只需要 Cargo。package.json 仅作为版本源和可选命令别名，仓库中不再包含 Web 前端或 Tauri 配置。
