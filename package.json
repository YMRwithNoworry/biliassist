{
  "name": "bilibili-account-manager",
  "version": "0.9.1",
  "description": "基于 GPUI 的 B站账号管理和自动回复原生桌面应用",
  "private": true,
  "scripts": {
    "dev": "cargo run --locked --manifest-path src-tauri/Cargo.toml",
    "build": "cargo build --release --locked --manifest-path src-tauri/Cargo.toml",
    "check": "cargo check --locked --manifest-path src-tauri/Cargo.toml",
    "test": "cargo test --locked --manifest-path src-tauri/Cargo.toml"
  }
}
